{
  "name": "leslie-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.4.0",
    "vite": "^5.0.0"
  }
}
